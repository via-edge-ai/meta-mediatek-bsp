require pumpkin-common.inc

MEDIATEK_SOC = "mt8516"

# Kernel
KERNEL_DEVICETREE = "${@bb.utils.contains('MACHINE_FEATURES', 'vesper-hat', 'mediatek/pumpkin8516_emmc_vesper.dtb', 'mediatek/pumpkin8516_emmc_yocto.dtb', d)}"
