require pumpkin-common.inc

MEDIATEK_SOC = "mt8167s"

# Kernel
KERNEL_DEVICETREE = "${@bb.utils.contains('MACHINE_FEATURES', 'vesper-hat', 'mediatek/pumpkin8167s_emmc_vesper.dtb', 'mediatek/pumpkin8167s_emmc_yocto.dtb', d)}"
